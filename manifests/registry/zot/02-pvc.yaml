apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zot-storage
  namespace: registry
  labels:
    app: zot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
