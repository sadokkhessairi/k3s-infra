apiVersion: v1
kind: Service
metadata:
  name: zot
  namespace: registry
  labels:
    app: zot
spec:
  type: NodePort
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    nodePort: 30500
    protocol: TCP
  selector:
    app: zot
